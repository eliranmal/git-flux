#!/usr/bin/env sh


has() {
	local item=$1; shift
	echo " $@ " | grep -q " $(escape $item) "
}

is() {
	[[ -n $1 ]]
}

escape() {
	echo "$1" | sed 's/\([\.\$\*]\)/\\\1/g'
}

is_available() {
	type $1 >/dev/null 2>&1
}

strip_prefix() {
	echo "$1" | sed -e 's,^'"$2"',,'
}
